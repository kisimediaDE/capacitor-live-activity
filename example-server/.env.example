# Path to your Firebase service account JSON (or rely on ADC in Cloud)
GOOGLE_APPLICATION_CREDENTIALS=/absolute/path/to/service-account.json

# Optional: HTTP server port
PORT=3000

# Default attributes type used by your widget's ActivityAttributes
# Must match your Swift type visible to the Widget target (often just "GenericAttributes").
ATTRIBUTES_TYPE=GenericAttributes